{
  "name": "f1-lightsout-game",
  "version": "1.0.0",
  "private": true,
  "description": "F1 Lights Out reaction time game — Cloudflare Pages + Workers + D1",
  "scripts": {
    "dev": "npx wrangler pages dev . --kv LEADERBOARD --d1 DB",
    "deploy:pages": "npx wrangler pages deploy . --project-name f1-lightsout",
    "deploy:worker": "cd worker && npx wrangler deploy",
    "kv:create": "npx wrangler kv namespace create LEADERBOARD",
    "db:create": "npx wrangler d1 create f1-lightsout-db",
    "db:init": "npx wrangler d1 execute f1-lightsout-db --file=./schema.sql",
    "db:init:local": "npx wrangler d1 execute f1-lightsout-db --local --file=./schema.sql"
  }
}
